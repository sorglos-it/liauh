# ============================================================================
# LIAUH Configuration - System Scripts
# Path prefix: scripts/
# ============================================================================

scripts:
  ubuntu:
    description: "Ubuntu system management (updates & Pro)"
    category: "System Updates"
    file: "ubuntu.sh"
    needs_sudo: true
    os_only: ubuntu
    
    actions:
      - name: "update"
        parameter: "update"
        description: "Update system (handles 25.04 → 25.10 upgrades)"
        prompts: []
      
      - name: "ubuntu-pro"
        parameter: "pro"
        description: "Attach Ubuntu Pro subscription"
        prompts:
          - question: "Ubuntu Pro subscription token?"
            variable: "KEY"
            type: "text"
            default: ""

  ca-cert-update:
    description: "Install CA certificate from a server"
    category: "Security"
    file: "ca-cert-update.sh"
    needs_sudo: true
    os_family:
      - debian
      - redhat
    
    actions:
      - name: "update"
        parameter: "update"
        prompts:
          - question: "CA server hostname? (e.g., cert.my.lan)"
            variable: "SERVER"
            type: "text"
            default: "cert.my.lan"
